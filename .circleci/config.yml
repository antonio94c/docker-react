language: generic
sudo: required  #permessi di root
services:
  - docker      #abbiamo bisogno di docker

before_install: #operazioni preliminari (build)
  - docker build -t antonio94c/docker-react -f Dockerfile.dev .

script: #scipt after install
  - docker run -e CI=true antonio94c/docker-react npm run test -- --coverage